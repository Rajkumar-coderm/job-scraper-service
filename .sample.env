APP_NAME=Job Scraper API

# Local only — on Render/cloud, headless is forced automatically
DEBUG=false
HEADLESS=false

SCRAPE_ON_STARTUP=false

# Local: parallel scrapers. Render: auto-enabled via IS_CLOUD detection
SCRAPE_SEQUENTIAL=false

ENRICH_HR_LIMIT=5

# --- Render dashboard (optional — auto-detected if RENDER=true) ---
# HEADLESS=true
# SCRAPE_SEQUENTIAL=true
# SCRAPE_ON_STARTUP=false
